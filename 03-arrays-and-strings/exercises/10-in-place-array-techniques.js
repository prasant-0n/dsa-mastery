// Phase 03.10 — In-Place Array Techniques
// Intentionally UNSOLVED.

function reverseInPlace(arr) { // TODO }
function swapInPlace(arr, i, j) { // TODO }
function removeElementInPlace(arr, target) { // TODO }
function removeDuplicatesSortedInPlace(arr) { // TODO }
function moveZeroesInPlace(arr) { // TODO }
function stableCompact(arr, predicate) { // TODO }
function partitionInPlace(arr, predicate) { // TODO }
function rotateRightInPlace(arr, k) { // TODO }
function rotateLeftInPlace(arr, k) { // TODO }
function mergeIntoFirstArray(a, m, b, n) { // TODO }
function transposeSquareInPlace(matrix) { // TODO }
function rotateMatrixInPlace(matrix) { // TODO }
function deduplicateInPlaceSorted(arr) { // TODO }
function insertionSortInPlace(arr) { // TODO }
function selectionSortInPlace(arr) { // TODO }
function insertionPositionInPlace(arr, value) { // TODO }
function compactObjectsInPlace(arr, predicate) { // TODO }
function analyzeMutationContract(arr, operation) { // TODO }
function proveOverwriteSafety(arr, operation) { // TODO }
function inPlaceStrategySynthesis(problem) { // TODO }

// For every solution document:
// - mutation contract
// - processed/unprocessed regions
// - invariant
// - overwrite-safety proof
// - stability requirements
// - time complexity
// - auxiliary vs output space
// - JavaScript allocation/retention considerations

// Self-check:
// [], [1], [1,2,3,4,5], [1,1,2,2,3],
// [0,0,1,0,3,12], [-3,-1,0,3,10],
// already sorted arrays, reverse-sorted arrays, duplicates,
// k=0, k=n, k>n, negative k, and matrices of size 1x1 and 3x3.
