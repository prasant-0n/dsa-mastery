// Phase 03.11 — Sorting + Array Patterns
// Intentionally UNSOLVED.

function numericSort(arr) { // TODO }
function sortWithoutMutating(arr) { // TODO }
function sortAndScanDuplicates(arr) { // TODO }
function closestPairDifference(arr) { // TODO }
function pairSumAfterSorting(arr, target) { // TODO }
function threeSum(arr, target) { // TODO }
function mergeIntervals(intervals) { // TODO }
function hasIntervalConflict(intervals) { // TODO }
function countValuesInRange(arr, left, right) { // TODO }
function countRangeUsingBounds(arr, left, right) { // TODO }
function topKBySorting(arr, k) { // TODO }
function topKUsingHeap(arr, k) { // TODO }
function kthSmallestWithSelection(arr, k) { // TODO }
function countingSortBounded(arr, maxValue) { // TODO }
function sortWithOriginalIndices(arr) { // TODO }
function stableSortRecords(records) { // TODO }
function groupEqualValuesAfterSort(arr) { // TODO }
function chooseSortOrHash(arr, constraints) { // TODO }
function chooseSortOrHeap(arr, k, constraints) { // TODO }
function sortingStrategySynthesis(problem) { // TODO }

// For every solution document:
// - why sorting is useful
// - sorting cost
// - post-sort pattern
// - invariant
// - mutation/stability requirements
// - duplicate and boundary semantics
// - complete time complexity
// - auxiliary/output space

// Self-check:
// [], [1], [3,1,2], [1,1,2,2,3], reverse-sorted arrays,
// negative values, intervals touching at endpoints, k=1/n,
// already sorted input, original-index preservation.
