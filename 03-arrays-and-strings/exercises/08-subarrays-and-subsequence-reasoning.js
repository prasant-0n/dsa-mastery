// Phase 03.8 — Subarrays & Subsequence Reasoning
// Intentionally UNSOLVED.

function enumerateSubarrays(arr) { // TODO }
function allSubarraySumsBruteForce(arr) { // TODO }
function allSubarraySumsIncremental(arr) { // TODO }
function rangeSumUsingPrefix(arr, queries) { // TODO }
function maxSubarraySum(arr) { // TODO }
function maxSubarrayWithIndices(arr) { // TODO }
function maxFixedWindowSum(arr, k) { // TODO }
function minLengthSubarrayAtLeastTarget(arr, target) { // TODO }
function countSubarraysWithSumK(arr, k) { // TODO }
function longestZeroSumSubarray(arr) { // TODO }
function longestEqualZeroOneSubarray(arr) { // TODO }
function isSubsequence(pattern, text) { // TODO }
function countSubsequencesWithTargetSum(arr, target) { // TODO }
function generateAllSubsequences(arr) { // TODO }
function longestIncreasingSubsequence(arr) { // TODO }
function chooseSubarrayStrategy(arr, problem) { // TODO }
function compareSubarrayBruteForceAndPrefix(arr) { // TODO }
function proveKadaneInvariant(arr) { // TODO }
function analyzeCandidateSpace(n) { // TODO }
function subarraySubsequenceStrategySynthesis(problem) { // TODO }

// For every solution document:
// - contiguous vs non-contiguous contract
// - state representation
// - invariant
// - why skipped/rejected candidates are safe
// - time complexity
// - auxiliary/output space
// - edge cases and empty-input semantics

// Self-check: [], [1], [-2,-1,-3], [1,-1,2,-2,3],
// [0,0,0], [1,2,3,4], mixed positive/negative values,
// fixed-window sizes 1/n/out-of-range, duplicate values.
