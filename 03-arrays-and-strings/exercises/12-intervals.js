// Phase 03.12 — Intervals
// Intentionally UNSOLVED.

function mergeIntervals(intervals) { // TODO }
function insertInterval(intervals, newInterval) { // TODO }
function hasOverlap(intervals) { // TODO }
function intervalIntersection(a, b) { // TODO }
function intervalUnion(intervals) { // TODO }
function maxConcurrentIntervals(intervals) { // TODO }
function minMeetingRooms(intervals) { // TODO }
function intervalScheduling(intervals) { // TODO }
function weightedIntervalScheduling(intervals) { // TODO }
function buildSweepEvents(intervals) { // TODO }
function sweepLineCoverage(intervals) { // TODO }
function countIntervalsContainingPoint(intervals, point) { // TODO }
function differenceArrayCoverage(intervals, maxCoordinate) { // TODO }
function coordinateCompressIntervals(intervals) { // TODO }
function clipIntervals(intervals, left, right) { // TODO }
function normalizeHalfOpenInterval(interval) { // TODO }
function chooseIntervalStrategy(intervals, problem) { // TODO }
function analyzeStaticVsDynamicIntervals(intervals, workload) { // TODO }
function proveMergeInvariant(intervals) { // TODO }
function intervalStrategySynthesis(problem) { // TODO }

// For every solution document:
// - endpoint semantics (closed/open/half-open)
// - sorting key
// - active-state invariant
// - event tie-breaking
// - correctness argument
// - time complexity
// - auxiliary/output space
// - static vs dynamic assumptions

// Self-check:
// [], [[1,2]], overlapping intervals, nested intervals,
// touching endpoints, duplicate intervals, zero-length intervals,
// already sorted/unsorted input, two interval lists, and large coordinates.
