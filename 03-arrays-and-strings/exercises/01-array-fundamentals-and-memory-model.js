// Phase 03.1 — Array Fundamentals & Memory Model
// These exercises are intentionally UNSOLVED.
// For every solution, record time and auxiliary-space complexity.

// 01 — Return arr[index]. Define behavior for an invalid index.
function getAt(arr, index) {
  // TODO
}

// 02 — Return the last element. Handle an empty array.
function getLast(arr) {
  // TODO
}

// 03 — Copy arr in forward order without map().
function copyForward(arr) {
  // TODO
}

// 04 — Copy arr in reverse order without reverse().
function copyReverse(arr) {
  // TODO
}

// 05 — Return the sum of numeric elements. Define empty-input behavior.
function sumArray(arr) {
  // TODO
}

// 06 — Find the maximum without sorting. Define empty-input behavior.
function findMax(arr) {
  // TODO
}

// 07 — Find the minimum without sorting.
function findMin(arr) {
  // TODO
}

// 08 — Return the first index containing target, or -1.
function linearSearch(arr, target) {
  // TODO
}

// 09 — Count occurrences of target.
function countOccurrences(arr, target) {
  // TODO
}

// 10 — Replace every oldValue with newValue in place and return arr.
function replaceInPlace(arr, oldValue, newValue) {
  // TODO
}

// 11 — Return a shallow copy without mutating arr.
function shallowCopy(arr) {
  // TODO
}

// 12 — Demonstrate that assignment creates an alias to the same array.
function demonstrateAliasing(arr) {
  // TODO
}

// 13 — Compare two arrays by corresponding content without JSON.stringify().
function arraysEqual(a, b) {
  // TODO
}

// 14 — Insert value at index without splice(); do not mutate arr.
function insertAt(arr, index, value) {
  // TODO
}

// 15 — Remove index without splice(); do not mutate arr.
function removeAt(arr, index) {
  // TODO
}

// 16 — Return a new array with the last element moved to the front.
function rotateRightByOne(arr) {
  // TODO
}

// 17 — Reverse arr in place, without reverse(). Target O(n) time/O(1) aux space.
function reverseInPlace(arr) {
  // TODO
}

// 18 — Return true when arr is non-decreasing. Empty/singleton = sorted.
function isSortedAscending(arr) {
  // TODO
}

// 19 — Return matrix[row][col], handling invalid positions explicitly.
function getMatrixCell(matrix, row, col) {
  // TODO
}

// 20 — Return complexity answers for indexed access, full scan, front insertion,
// pop, shallow copy, and scanning an R x C matrix.
function arrayComplexityAnswers() {
  // TODO
}

// Self-check inputs:
// [], [1], [1,2], [5,5,5], [1,2,3,4], [4,3,2,1],
// [-3,0,7,-1], [1,2,1,2,1]
// Explain the invariant/correctness idea for each non-trivial solution.
