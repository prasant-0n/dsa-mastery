// Phase 03.15 — String Matching Foundations
// Intentionally UNSOLVED.

function naiveContains(text, pattern) { // TODO }
function naiveIndexOf(text, pattern) { // TODO }
function firstMatchIndex(text, pattern) { // TODO }
function allMatchIndices(text, pattern) { // TODO }
function countOverlappingMatches(text, pattern) { // TODO }
function countNonOverlappingMatches(text, pattern) { // TODO }
function startsWithPattern(text, pattern) { // TODO }
function endsWithPattern(text, pattern) { // TODO }
function matchUsingIndices(text, pattern) { // TODO }
function findPatternOccurrences(text, pattern) { // TODO }
function longestBorder(s) { // TODO }
function buildPrefixTable(pattern) { // TODO }
function explainNaiveMismatch(text, pattern, start) { // TODO }
function compareNaiveAndPrefixMatching(text, pattern) { // TODO }
function rollingHashMatch(text, pattern) { // TODO }
function verifyHashCandidate(text, pattern, index) { // TODO }
function multiPatternSearch(text, patterns) { // TODO }
function chooseMatchingStrategy(text, pattern, workload) { // TODO }
function matchingComplexityAnalysis(text, pattern, workload) { // TODO }
function stringMatchingStrategySynthesis(problem) { // TODO }

// For every solution document:
// - empty text/pattern semantics
// - exact character model
// - overlapping vs non-overlapping behavior
// - invariant/state
// - time complexity in N and M
// - auxiliary space
// - substring/allocation costs
// - preprocessing/query trade-offs
// - collision/correctness considerations for hashing

// Self-check:
// "", "a", pattern longer than text, identical strings,
// repeated patterns, overlapping matches such as "aaaa" / "aa",
// no match, match at index 0, match at the end, and large inputs.
