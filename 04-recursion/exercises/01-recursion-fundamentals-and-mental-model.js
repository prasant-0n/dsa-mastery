// Phase 04.1 — Recursion Fundamentals & Mental Model
// Intentionally UNSOLVED.

function countdown(n) { // TODO }
function countUp(n) { // TODO }
function sumTo(n) { // TODO }
function factorial(n) { // TODO }
function power(base, exponent) { // TODO }
function productRange(left, right) { // TODO }
function sumArray(arr, index = 0) { // TODO }
function findMaxRecursive(arr, index = 0) { // TODO }
function countOccurrences(arr, target, index = 0) { // TODO }
function reverseStringRecursive(s, index = 0) { // TODO }
function isPalindromeRecursive(s, left = 0, right = s.length - 1) { // TODO }
function binarySearchRecursive(arr, target, left = 0, right = arr.length - 1) { // TODO }
function sumNestedArray(value) { // TODO }
function flattenNestedArray(value) { // TODO }
function traceRecursiveCalls(n) { // TODO }
function recursiveDepthAnalysis(n) { // TODO }
function compareRecursiveAndIterative(n) { // TODO }
function analyzeSliceVsIndexRecursion(arr) { // TODO }
function proveRecursiveTermination(problem) { // TODO }
function recursionStrategySynthesis(problem) { // TODO }

// Self-check:
// - base case is correct and reachable
// - every recursive call makes measurable progress
// - return values propagate correctly
// - empty/singleton inputs
// - negative/zero values where applicable
// - duplicates
// - already-solved inputs
// - very deep recursion
// - shared/mutated state
// - repeated subproblems
// - slice/copy allocations
// - recursive vs iterative trade-offs
// - time complexity
// - stack-space complexity
// - correctness and termination reasoning

// Do not solve these from memory alone.
// For each exercise first write:
// 1. State
// 2. Base case
// 3. Progress measure
// 4. Recursive transition
// 5. Return/combine step
// 6. Complexity
