// Phase 04.3 — Base Cases, Recursive Cases & Termination
// Intentionally UNSOLVED.

function factorialWithContract(n) { // TODO }
function sumToBoundary(n) { // TODO }
function countToBoundary(n, index = 0) { // TODO }
function recursiveArraySum(arr, index = 0) { // TODO }
function recursiveArrayMax(arr, index = 0) { // TODO }
function recursiveBinarySearch(arr, target, left = 0, right = arr.length - 1) { // TODO }
function recursivePalindrome(s, left = 0, right = s.length - 1) { // TODO }
function recursiveRangeProduct(left, right) { // TODO }
function recursivePower(base, exponent) { // TODO }
function identifyTerminalStates(problem) { // TODO }
function identifyProgressMeasure(problem) { // TODO }
function verifyTermination(problem) { // TODO }
function analyzeBranchTermination(problem) { // TODO }
function detectUnreachableBaseCase(problem) { // TODO }
function detectNonDecreasingRecursion(problem) { // TODO }
function designSafePruningRule(problem) { // TODO }
function recursiveSearchWithSuccessFailure(problem) { // TODO }
function analyzeMultiParameterProgress(problem) { // TODO }
function compareRecursiveAndIterativeTermination(problem) { // TODO }
function terminationStrategySynthesis(problem) { // TODO }

// Self-check:
// - distinguish validation errors from algorithmic base cases
// - identify every terminal state
// - prove each base case is reachable
// - identify a strict progress measure
// - verify every recursive branch progresses
// - test empty/singleton/boundary inputs
// - test invalid/no-solution states
// - detect transitions that move away from the base case
// - check repeated states
// - prove pruning is safe before using it
// - separate termination from efficiency
// - estimate maximum recursion depth
// - consider JavaScript call-stack limits
// - compare recursive and iterative alternatives

// For every exercise write before coding:
// 1. State
// 2. Base/terminal states
// 3. Recursive transition
// 4. Progress measure
// 5. Termination argument
// 6. Correctness argument
// 7. Time complexity
// 8. Stack-space complexity
