// Phase 04.2 — Recursion State, Call Stack & Execution Tracing
// Intentionally UNSOLVED.

function traceCountdown(n) { // TODO }
function traceCountUp(n) { // TODO }
function traceDownAndUp(n) { // TODO }
function traceReturnValues(n) { // TODO }
function traceFactorial(n) { // TODO }
function traceSumArray(arr, index = 0) { // TODO }
function traceStringReverse(s, index = 0) { // TODO }
function recursionDepthLinear(n) { // TODO }
function recursionDepthBinarySearch(arr, target, left = 0, right = arr.length - 1) { // TODO }
function countRecursiveCalls(n) { // TODO }
function countFibonacciCalls(n) { // TODO }
function analyzeCallTree(n) { // TODO }
function distinguishLocalAndSharedState(state, depth) { // TODO }
function traceBacktrackingState(choices) { // TODO }
function traceAccumulator(n, total = 0) { // TODO }
function compareIndexAndSliceRecursion(arr) { // TODO }
function identifyBaseCases(problem) { // TODO }
function identifyProgressMeasure(problem) { // TODO }
function debugFirstIncorrectRecursiveState(problem) { // TODO }
function recursionExecutionSynthesis(problem) { // TODO }

// Self-check:
// - trace both CALL and RETURN phases
// - record state at every frame
// - distinguish maximum depth from total calls
// - identify local vs shared state
// - verify return-value propagation
// - identify all base cases
// - prove progress toward termination
// - inspect branching recursion trees
// - identify repeated states/calls
// - account for array/string allocations
// - account for JavaScript call-stack limits
// - compare recursive and iterative alternatives
// - derive time and stack-space complexity

// For each exercise, write the trace before implementing the function.
