// Phase 04.5 — Recursion Patterns: Linear, Binary & Multiple-Branch Recursion
// Intentionally UNSOLVED.

function linearRecursiveSum(n) { // TODO }
function linearArrayTraversal(arr, index = 0) { // TODO }
function logarithmicRecursiveSearch(arr, target, left = 0, right = arr.length - 1) { // TODO }
function countLinearRecursiveCalls(n) { // TODO }
function binaryRecursiveCallCount(n) { // TODO }
function fibonacciRecursiveCalls(n) { // TODO }
function subsetGeneration(arr, index = 0, current = []) { // TODO }
function permutationGeneration(arr, index = 0) { // TODO }
function analyzeBranchingFactor(problem) { // TODO }
function analyzeRecursionDepth(problem) { // TODO }
function analyzeRecursionTree(problem) { // TODO }
function analyzeWorkPerLevel(problem) { // TODO }
function compareRecursionTreeAndStateGraph(problem) { // TODO }
function identifyRepeatedRecursiveStates(problem) { // TODO }
function designMemoizedRecursion(problem) { // TODO }
function analyzePruningEffect(problem) { // TODO }
function compareLinearAndBranchingRecursion(problem) { // TODO }
function deriveRecurrenceForPattern(problem) { // TODO }
function chooseRecursivePattern(problem) { // TODO }
function recursionPatternSynthesis(problem) { // TODO }

// Self-check:
// - count recursive calls separately from recursion depth
// - identify the state shrink rate
// - estimate branching factor and depth
// - distinguish recursion tree from unique-state graph
// - detect repeated states
// - separate unavoidable output cost from redundant work
// - analyze work performed outside recursive calls
// - analyze stack space independently from time
// - test empty/singleton/boundary inputs
// - test pathological branching
// - verify pruning is safe
// - compare memoized and non-memoized behavior
// - consider JavaScript call-stack limits
// - connect the pattern to backend/AI search workloads

// Before coding each exercise, write:
// 1. Recursive state
// 2. Base cases
// 3. Branch count
// 4. Child-state size
// 5. Progress measure
// 6. Recurrence or tree model
// 7. Time complexity
// 8. Stack-space complexity
