// Phase 05.02 — Linked List Traversal & Pointer Manipulation
// Intentionally UNSOLVED.

function traverseList(head) { // TODO }
function listToArrayWithIndex(head) { // TODO }
function findNode(head, target) { // TODO }
function findPredecessor(head, target) { // TODO }
function findMiddleFirst(head) { // TODO }
function findMiddleSecond(head) { // TODO }
function detectCycleFloyd(head) { // TODO }
function detectCycleWithSet(head) { // TODO }
function reverseListIterative(head) { // TODO }
function reverseListRecursive(head) { // TODO }
function deleteTargetWithPredecessor(head, target) { // TODO }
function insertAfterTarget(head, target, value) { // TODO }
function deleteTargetWithDummy(head, target) { // TODO }
function reverseValuesWithoutMutation(head) { // TODO }
function sumList(head) { // TODO }
function findFirstMatchingNode(head, predicate) { // TODO }
function validateTraversalInvariant(head) { // TODO }
function analyzePointerMutation(operation) { // TODO }
function compareRecursiveAndIterativeTraversal(head) { // TODO }
function traversalAndPointerSynthesis(problem) { // TODO }

// Self-check:
// - handle empty, singleton, and two-node lists
// - maintain a clear traversal invariant
// - preserve next before pointer rewiring
// - preserve reachability of the unprocessed suffix
// - distinguish first vs second middle on even-length lists
// - update head correctly after mutations
// - detect accidental cycles
// - analyze recursive stack vs iterative auxiliary space
// - separate output space from auxiliary space
// - draw previous/current/next before difficult rewiring
