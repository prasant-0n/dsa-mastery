// Phase 05.03 — Linked List Insertion, Deletion & Mutation
// Intentionally UNSOLVED.

function insertAtHead(head, value) { // TODO }
function appendWithTail(list, value) { // TODO }
function appendWithoutTail(head, value) { // TODO }
function insertAfterNode(node, value) { // TODO }
function insertBeforeValue(head, target, value) { // TODO }
function deleteHead(list) { // TODO }
function deleteFirstValue(list, target) { // TODO }
function deleteAtIndex(list, index) { // TODO }
function deleteAllValues(list, target) { // TODO }
function deleteTail(list) { // TODO }
function deleteWithDummy(head, target) { // TODO }
function removeConsecutiveMatches(head, target) { // TODO }
function spliceAfterNode(node, segmentHead, segmentTail) { // TODO }
function concatenateLists(listA, listB) { // TODO }
function isolateRemovedNode(node) { // TODO }
function validateSinglyListMetadata(list) { // TODO }
function validateDoublyListMetadata(list) { // TODO }
function detectMutationCycle(head) { // TODO }
function compareMutableAndPersistentInsertion(problem) { // TODO }
function linkedListMutationSynthesis(problem) { // TODO }

// Self-check:
// - draw before/after pointer states
// - save successors before overwriting links
// - handle empty and singleton lists
// - update head, tail, and size exactly once
// - do not skip consecutive matching nodes
// - distinguish singly and doubly linked invariants
// - consider aliasing and shared-node ownership
// - detect accidental cycles after complex rewiring
// - analyze complexity based on already-known references
// - compare mutation with non-mutating/persistent alternatives
