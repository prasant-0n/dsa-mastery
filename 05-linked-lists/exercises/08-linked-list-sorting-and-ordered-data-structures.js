// Phase 05.08 — Linked List Sorting & Ordered Data Structures
// Intentionally UNSOLVED.

function insertionSortList(head, compare) { // TODO }
function mergeSortedListsForSort(headA, headB, compare) { // TODO }
function mergeSortList(head, compare) { // TODO }
function bottomUpMergeSortList(head, compare) { // TODO }
function splitForMergeSort(head) { // TODO }
function insertIntoOrderedList(head, node, compare) { // TODO }
function deleteFromOrderedList(head, target, compare) { // TODO }
function stableInsertIntoOrderedList(head, node, compare) { // TODO }
function validateOrderedInvariant(head, compare) { // TODO }
function findOrderedInsertionPoint(head, node, compare) { // TODO }
function sortWithoutCreatingNodes(head, compare) { // TODO }
function sortByKey(head, keySelector) { // TODO }
function analyzeSortingStability(original, sorted, keySelector) { // TODO }
function compareInsertionSortAndMergeSort(head, compare) { // TODO }
function analyzeNearlySortedList(head, compare) { // TODO }
function validateAcyclicInputBeforeSort(head) { // TODO }
function analyzeSortingOwnership(head, externalReferences) { // TODO }
function compareOrderedListVsHeap(problem) { // TODO }
function compareOrderedListVsBalancedTree(problem) { // TODO }
function linkedListSortingSynthesis(problem) { // TODO }

// Self-check:
// - exploit sequential access rather than array-style indexing
// - preserve node identity when required
// - define stable duplicate ordering explicitly
// - support comparator-based sorting
// - split lists safely for merge sort
// - terminate recursive calls with smaller inputs
// - consider bottom-up merge sort for O(1) auxiliary space
// - reject or explicitly handle cyclic input
// - reason about shared-node ownership and aliasing
// - select list vs heap vs tree from the workload
